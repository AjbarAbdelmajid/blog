{% include "base.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register container example-wrapper'}}) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-primary"  value="register" />
    </div>
{{ form_end(form) }}
