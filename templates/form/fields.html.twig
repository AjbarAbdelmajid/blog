{% extends 'form_div_layout.html.twig' %}

{% block file_widget %}

    {% spaceless %}
        
    {% if media|default(null) %}
        {% if media is not null %}
            <img src="{% path media, 'reference' %}" width="100" height="100"/><br><br>
        {% endif %}
    {% endif %}

    {{ block('form_widget') }}
    {% endspaceless %}
{% endblock %}
